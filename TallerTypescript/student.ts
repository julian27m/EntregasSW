export class student {
    code: number;
    id: number;
    age: number;
    adress: string;
    phone: number;
  
    constructor(code: number, id: number, age: number, adress: string, phone: number) {
      this.code = code;
      this.id = id;
      this.age = age;
      this.adress = adress;
      this.phone = phone;
    }
  }